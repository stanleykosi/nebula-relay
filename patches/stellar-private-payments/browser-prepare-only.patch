diff --git a/app/crates/platforms/web/src/client/mod.rs b/app/crates/platforms/web/src/client/mod.rs
index abfcab3..6b86583 100644
--- a/app/crates/platforms/web/src/client/mod.rs
+++ b/app/crates/platforms/web/src/client/mod.rs
@@ -30,6 +30,8 @@ mod transact;

 const CONFIRM_POLL_ATTEMPTS: u32 = 30;
 const CONFIRM_POLL_INTERVAL_MS: u32 = 1_000;
+const PROVER_LOAD_TIMEOUT_MS: u32 = 180_000;
+const PROVER_TRANSACT_TIMEOUT_MS: u32 = 10 * 60_000;

 fn execute_hashes_to_js(result: Option<Vec<String>>) -> Result<JsValue, JsError> {
     match result {
@@ -38,6 +40,13 @@ fn execute_hashes_to_js(result: Option<Vec<String>>) -> Result<JsValue, JsError>
     }
 }

+fn prepared_prover_tx_to_js(result: Option<PreparedProverTx>) -> Result<JsValue, JsError> {
+    match result {
+        None => Ok(JsValue::NULL),
+        Some(prepared) => Ok(serde_wasm_bindgen::to_value(&prepared)?),
+    }
+}
+
 fn emit_progress(
     on_status: &Option<Function>,
     flow: &'static str,
@@ -212,7 +221,7 @@ impl WebClient {

     pub async fn ping_prover(&self) -> anyhow::Result<()> {
         let mut bridge = self.prover_bridge.fork();
-        let resp = with_timeout(5_000, bridge.run(ProverWorkerRequest::Ping)).await?;
+        let resp = with_timeout(PROVER_LOAD_TIMEOUT_MS, bridge.run(ProverWorkerRequest::Ping)).await?;
         match resp {
             ProverWorkerResponse::Pong => Ok(()),
             ProverWorkerResponse::Error(e) => Err(anyhow::anyhow!(e)),
@@ -631,6 +640,28 @@ impl WebClient {
         execute_hashes_to_js(result)
     }

+    #[wasm_bindgen(js_name = prepareDeposit)]
+    #[allow(clippy::too_many_arguments)]
+    pub async fn prepare_deposit(
+        &self,
+        pool_contract_id: String,
+        user_address: String,
+        amount: BigInt,
+        output_amounts: Array,
+        on_status: Option<Function>,
+    ) -> Result<JsValue, JsError> {
+        let result = self
+            .prepare_deposit_inner(
+                pool_contract_id,
+                user_address,
+                amount,
+                output_amounts,
+                on_status,
+            )
+            .await?;
+        prepared_prover_tx_to_js(result)
+    }
+
     #[wasm_bindgen(js_name = plan)]
     pub async fn plan(
         &self,
@@ -740,6 +771,37 @@ impl WebClient {
         execute_hashes_to_js(result)
     }

+    #[wasm_bindgen(js_name = prepareTransact)]
+    #[allow(clippy::too_many_arguments)]
+    pub async fn prepare_transact_wasm(
+        &self,
+        pool_contract_id: String,
+        user_address: String,
+        ext_recipient: String,
+        ext_amount: BigInt,
+        input_note_ids: Array,
+        output_amounts: Array,
+        out_recipient_note_keys_hex: Array,
+        out_recipient_enc_keys_hex: Array,
+        on_status: Option<Function>,
+    ) -> Result<JsValue, JsError> {
+        let result = self
+            .prepare_transact_inner(
+                pool_contract_id,
+                user_address,
+                ext_recipient,
+                ext_amount,
+                input_note_ids,
+                output_amounts,
+                out_recipient_note_keys_hex,
+                out_recipient_enc_keys_hex,
+                on_status,
+                "prepareTransact",
+            )
+            .await?;
+        prepared_prover_tx_to_js(result)
+    }
+
     #[wasm_bindgen(js_name = generateSelectiveDisclosure)]
     #[allow(clippy::too_many_arguments)]
     pub async fn generate_selective_disclosure(
diff --git a/app/crates/platforms/web/src/client/transact.rs b/app/crates/platforms/web/src/client/transact.rs
index a2736c7..0fffb7e 100644
--- a/app/crates/platforms/web/src/client/transact.rs
+++ b/app/crates/platforms/web/src/client/transact.rs
@@ -2,7 +2,7 @@
 //! `executeTransfer`, `executeWithdraw`).

 use super::{
-    WebClient, emit_progress, parse_ext_amount_decimal, parse_input_note_ids,
+    PROVER_TRANSACT_TIMEOUT_MS, WebClient, emit_progress, parse_ext_amount_decimal, parse_input_note_ids,
     parse_note_amount_decimal, parse_output_amounts, parse_output_recipient_keys,
     parse_u32_decimal, sign::sign_prepared_transaction,
 };
@@ -207,7 +207,7 @@ impl WebClient {
             .map_err(|e| JsError::new(&format!("failed to load prover: {e:?}")))?;

         let prepared = match self
-            .prover_request(ProverWorkerRequest::Transact(params), 20_000)
+            .prover_request(ProverWorkerRequest::Transact(params), PROVER_TRANSACT_TIMEOUT_MS)
             .await?
         {
             ProverWorkerResponse::TransactPrepared(p) => p,
@@ -299,6 +299,57 @@ impl WebClient {
         }))
     }

+    #[allow(clippy::too_many_arguments)]
+    pub(super) async fn prepare_transact_inner(
+        &self,
+        pool_contract_id: String,
+        user_address: String,
+        ext_recipient: String,
+        ext_amount: BigInt,
+        input_note_ids: Array,
+        output_amounts: Array,
+        out_recipient_note_keys_hex: Array,
+        out_recipient_enc_keys_hex: Array,
+        on_status: Option<Function>,
+        flow: &'static str,
+    ) -> Result<Option<PreparedProverTx>, JsError> {
+        let expected_outputs =
+            u32::try_from(N_OUTPUTS).map_err(|_| JsError::new("N_OUTPUTS exceeds u32"))?;
+        if out_recipient_note_keys_hex.length() != expected_outputs {
+            return Err(JsError::new(&format!(
+                "out_recipient_note_keys_hex must have length {N_OUTPUTS}"
+            )));
+        }
+        if out_recipient_enc_keys_hex.length() != expected_outputs {
+            return Err(JsError::new(&format!(
+                "out_recipient_enc_keys_hex must have length {N_OUTPUTS}"
+            )));
+        }
+
+        let ext_amount = parse_ext_amount_decimal(&ext_amount)?;
+        let input_commitments = parse_input_note_ids(
+            &input_note_ids,
+            0,
+            2,
+            "input_note_ids must have length 0..=2",
+        )?;
+        let out_amounts = parse_output_amounts(&output_amounts)?;
+        let (out_note_pks, out_enc_pks) =
+            parse_output_recipient_keys(&out_recipient_note_keys_hex, &out_recipient_enc_keys_hex)?;
+
+        let step = Transact::new(
+            input_commitments,
+            out_amounts,
+            ext_amount,
+            ext_recipient,
+            out_note_pks,
+            out_enc_pks,
+        );
+
+        self.prove_transact_inner(&pool_contract_id, &user_address, &step, flow, &on_status)
+            .await
+    }
+
     #[allow(clippy::too_many_arguments)]
     pub(super) async fn execute_transact_inner(
         &self,
@@ -356,6 +407,57 @@ impl WebClient {
         self.execute_transact(ctx, step, flow).await
     }

+    #[allow(clippy::too_many_arguments)]
+    pub(super) async fn prepare_deposit_inner(
+        &self,
+        pool_contract_id: String,
+        user_address: String,
+        amount: BigInt,
+        output_amounts: Array,
+        on_status: Option<Function>,
+    ) -> Result<Option<PreparedProverTx>, JsError> {
+        let ext_amount = parse_ext_amount_decimal(&amount)?;
+        if ext_amount <= ExtAmount::ZERO {
+            return Err(JsError::new("amount must be > 0 for deposit"));
+        }
+        let out_amounts = parse_output_amounts(&output_amounts)?;
+
+        let keys = match self
+            .storage_request(StorageWorkerRequest::UserKeys(user_address.clone()), 1_000)
+            .await?
+        {
+            StorageWorkerResponse::UserKeys(keys) => {
+                keys.ok_or_else(|| JsError::new("user keys not found in worker storage"))?
+            }
+            other => {
+                return Err(JsError::new(&format!(
+                    "Unexpected storage response loading user keys: {:?}",
+                    other
+                )));
+            }
+        };
+        let note_pk: NotePublicKey = keys.note_keypair.public;
+        let enc_pk: EncryptionPublicKey = keys.encryption_keypair.public;
+
+        let step = Transact::new(
+            Vec::new(),
+            out_amounts,
+            ext_amount,
+            pool_contract_id.clone(),
+            [Some(note_pk.clone()), Some(note_pk)],
+            [Some(enc_pk.clone()), Some(enc_pk)],
+        );
+
+        self.prove_transact_inner(
+            &pool_contract_id,
+            &user_address,
+            &step,
+            "prepareDeposit",
+            &on_status,
+        )
+        .await
+    }
+
     #[allow(clippy::too_many_arguments)]
     pub(super) async fn execute_deposit_inner(
         &self,
diff --git a/app/crates/platforms/web/src/workers/prover.rs b/app/crates/platforms/web/src/workers/prover.rs
index b45ec6b..a676f77 100644
--- a/app/crates/platforms/web/src/workers/prover.rs
+++ b/app/crates/platforms/web/src/workers/prover.rs
@@ -84,7 +84,7 @@ async fn load_circuit_artifacts() -> Result<(), JsError> {
     if WITNESS_CALC.with(|s| s.borrow().is_some()) && PROVER.with(|s| s.borrow().is_some()) {
         return Ok(());
     }
-    let (wasm_bytes, r1cs_bytes, disc_wasm_bytes, disc_r1cs_bytes) = try_join!(
+    let (wasm_bytes, r1cs_bytes) = try_join!(
         async {
             let wasm_bytes: Vec<u8> = fetch_circuit_file("circuits/policy_tx_2_2.wasm").await?;
             log::debug!(
@@ -100,29 +100,12 @@ async fn load_circuit_artifacts() -> Result<(), JsError> {
                 r1cs_bytes.len()
             );
             Ok::<Vec<u8>, JsError>(r1cs_bytes)
-        },
-        async {
-            let wasm_bytes: Vec<u8> =
-                fetch_circuit_file("circuits/selectiveDisclosure_1.wasm").await?;
-            log::debug!(
-                "[{WORKER_NAME}] fetched selectiveDisclosure_1.wasm: {} bytes",
-                wasm_bytes.len()
-            );
-            Ok::<Vec<u8>, JsError>(wasm_bytes)
-        },
-        async {
-            let r1cs_bytes: Vec<u8> =
-                fetch_circuit_file("circuits/selectiveDisclosure_1.r1cs").await?;
-            log::debug!(
-                "[{WORKER_NAME}] fetched selectiveDisclosure_1.r1cs: {} bytes",
-                r1cs_bytes.len()
-            );
-            Ok::<Vec<u8>, JsError>(r1cs_bytes)
         }
     )?;

-    // Integrity checks (regular builds): ensure we are using the exact
-    // artifact versions this binary was built against.
+    // Load and expose the transaction prover first. Deposit preparation does
+    // not need the selective-disclosure prover, so waiting for disclosure
+    // artifacts here makes the browser path unnecessarily fragile.
     ensure_sha256_matches(
         "policy_tx_2_2_proving_key.bin",
         PROVING_KEY,
@@ -142,6 +125,38 @@ async fn load_circuit_artifacts() -> Result<(), JsError> {
         crate::artifact_hashes::EXPECTED_POLICY_TX_2_2_R1CS_SHA256,
     )?;

+    let witness_calc = WitnessCalculator::new(&wasm_bytes, &r1cs_bytes)
+        .map_err(|e| JsError::new(&format!("failed to init witness calculator: {e:#}")))?;
+    let prover = Prover::new(PROVING_KEY, &r1cs_bytes).expect("FAILED Prover");
+
+    WITNESS_CALC.with(|cell| {
+        *cell.borrow_mut() = Some(witness_calc);
+    });
+    PROVER.with(|cell| {
+        *cell.borrow_mut() = Some(prover);
+    });
+
+    let (disc_wasm_bytes, disc_r1cs_bytes) = try_join!(
+        async {
+            let wasm_bytes: Vec<u8> =
+                fetch_circuit_file("circuits/selectiveDisclosure_1.wasm").await?;
+            log::debug!(
+                "[{WORKER_NAME}] fetched selectiveDisclosure_1.wasm: {} bytes",
+                wasm_bytes.len()
+            );
+            Ok::<Vec<u8>, JsError>(wasm_bytes)
+        },
+        async {
+            let r1cs_bytes: Vec<u8> =
+                fetch_circuit_file("circuits/selectiveDisclosure_1.r1cs").await?;
+            log::debug!(
+                "[{WORKER_NAME}] fetched selectiveDisclosure_1.r1cs: {} bytes",
+                r1cs_bytes.len()
+            );
+            Ok::<Vec<u8>, JsError>(r1cs_bytes)
+        }
+    )?;
+
     ensure_sha256_matches(
         "selectiveDisclosure_1_proving_key.bin",
         DISCLOSURE_PROVING_KEY,
@@ -161,10 +176,6 @@ async fn load_circuit_artifacts() -> Result<(), JsError> {
         crate::artifact_hashes::EXPECTED_SELECTIVE_DISCLOSURE_1_R1CS_SHA256,
     )?;

-    let witness_calc = WitnessCalculator::new(&wasm_bytes, &r1cs_bytes)
-        .map_err(|e| JsError::new(&format!("failed to init witness calculator: {e:#}")))?;
-    let prover = Prover::new(PROVING_KEY, &r1cs_bytes).expect("FAILED Prover");
-
     let disc_witness_calc =
         WitnessCalculator::new(&disc_wasm_bytes, &disc_r1cs_bytes).map_err(|e| {
             JsError::new(&format!(
@@ -174,12 +185,6 @@ async fn load_circuit_artifacts() -> Result<(), JsError> {
     let disc_prover =
         Prover::new(DISCLOSURE_PROVING_KEY, &disc_r1cs_bytes).expect("FAILED Disclosure Prover");

-    WITNESS_CALC.with(|cell| {
-        *cell.borrow_mut() = Some(witness_calc);
-    });
-    PROVER.with(|cell| {
-        *cell.borrow_mut() = Some(prover);
-    });
     DISCLOSURE_WITNESS_CALC.with(|cell| {
         *cell.borrow_mut() = Some(disc_witness_calc);
     });
